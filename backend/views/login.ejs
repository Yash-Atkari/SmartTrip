<!DOCTYPE html>
<html>
<head>
  <title>Login - SmartTrip</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    /* Body Gradient + Fullscreen */
    body {
      height: 100vh;
      margin: 0;
      font-family: 'Arial', sans-serif;
      background: url('/loginbg.jpg') no-repeat center center fixed;
      background-size: cover;
      display: flex;
      justify-content: center;
      align-items: center;
      overflow: hidden;
      position: relative;
    }

    /* Sparkles */
    .sparkle {
      position: absolute;
      font-size: 24px;
      animation: sparkle 1.2s infinite alternate;
    }

    @keyframes sparkle {
      0% { opacity: 0; transform: scale(0.5) rotate(0deg); }
      50% { opacity: 1; transform: scale(1.2) rotate(15deg); }
      100% { opacity: 0; transform: scale(0.5) rotate(30deg); }
    }

    /* Card */
    .login-card {
      background: white;
      border-radius: 25px;
      padding: 40px 30px;
      width: 360px;
      box-shadow: 0 15px 25px rgba(0,0,0,0.3);
      z-index: 1;
      position: relative;
    }

    .login-card h3 {
      text-align: center;
      margin-bottom: 25px;
      color: #333;
      font-weight: bold;
    }

    /* Input */
    .login-card input {
      width: 100%;
      padding: 12px 15px;
      border-radius: 12px;
      border: 1px solid #ccc;
      margin-bottom: 20px;
      font-size: 16px;
      outline: none;
      transition: 0.3s;
    }

    .login-card input:focus {
      border-color: #6a11cb;
      box-shadow: 0 0 8px rgba(106,17,203,0.4);
    }

    /* Button */
    .login-card button {
      width: 100%;
      padding: 12px;
      background: #6a11cb;
      color: white;
      border: none;
      border-radius: 25px;
      font-size: 16px;
      cursor: pointer;
      transition: background 0.3s;
    }

    .login-card button:hover {
      background: #2575fc;
    }

    /* Message Alert */
    .alert {
      background-color: #ffdddd;
      color: #d8000c;
      padding: 10px;
      border-radius: 8px;
      margin-bottom: 15px;
      text-align: center;
    }

    /* Signup Text */
    .signup-text {
      text-align: center;
      margin-top: 15px;
      color: #6a11cb;
    }

    .signup-text a {
      color: #2575fc;
      text-decoration: none;
      font-weight: bold;
    }

    .signup-text a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <!-- Sparkles -->
  <span class="sparkle" style="top: 20px; left: 40px;">✨</span>
  <span class="sparkle" style="top: 50px; right: 60px;">✨</span>
  <span class="sparkle" style="top: 80px; left: 120px;">✨</span>
  

  <!-- Login Card -->
  <div class="login-card">
    <h3>Login</h3>
    <% if (message && message.length > 0) { %>
      <div class="alert"><%= message %></div>
    <% } %>
    <form action="/api/auth/login" method="POST">
      <input type="text" name="username" placeholder="Username" required />
      <input type="password" name="password" placeholder="Password" required />
      <button type="submit">Login</button>
    </form>
    <p class="signup-text">Don’t have an account? <a href="/api/auth/signup">Sign up</a></p>
  </div>
</body>
</html>
